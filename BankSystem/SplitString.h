#ifndef SPLIT_Header
#define SPLIT_Header

#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<string> Split(int);
vector<string> Split(double);
vector<string> SplitDate(string);

#endif